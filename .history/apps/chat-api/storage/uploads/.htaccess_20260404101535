# Block ALL script execution in upload directory

php_flag engine off

# Deny direct access — files served only through download controller

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
